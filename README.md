# maven-tomcat-without-jenkins

# To check the sytanx of pom.xml say
    $ mvn validate

# To generate the artifact
    $ mvn package

# To deploy the generated artifact to container like tomcat
    $ mvn tomcat7:deploy 
